# Copie ce fichier en .env pour le développement local (netlify dev).
# Ne jamais committer le vrai fichier .env — mets les vraies valeurs dans
# Netlify (Site settings → Environment variables) pour la production.

STRIPE_SECRET_KEY=sk_test_...
SITE_URL=http://localhost:8888
